{
  "name": "budgeting",
  "version": "1.0.0",
  "author": "TheLocust3 <jake.kinsella@gmail.com>",
  "license": "UNLICENSED",
  "scripts": {
    "lint": "eslint \"**/*.{js,ts}\" --quiet",
    "setup": "cd magic && yarn link && yarn build && cd ../model && yarn link magic && yarn link && yarn build && cd ../engine && yarn link magic && yarn link model && cd ../logic && yarn link magic && yarn link model && cd ../scheduler && yarn link magic && yarn link model",
    "clean": "rm -rf magic/dist && rm -rf model/dist && rm -rf engine/dist && rm -rf logic/dist && rm -rf scheduler/dist",
    "test": "jest --forceExit --verbose \".*/test/system/.*\"",
    "start": "concurrently -k -p \"[{name}]\" -n \"engine,logic,scheduler,magic,model\" -c \"cyan.bold,green.bold,orange.bold,purple.bold,yellow.bold\" \"cd engine && yarn start\" \"cd logic && yarn start\" \"cd scheduler && yarn start\" \"cd magic && yarn start\" \"cd model && yarn start\"",
    "migrate": "cd engine && yarn migrate && cd ../scheduler && yarn migrate && cd ../logic && yarn migrate",
    "rollback": "cd logic && yarn rollback && cd ../scheduler && yarn rollback && cd ../engine && yarn rollback"
  },
  "devDependencies": {
    "@types/jest": "^27.4.0",
    "concurrently": "^7.0.0",
    "jest": "^27.4.7",
    "ts-jest": "^27.1.2"
  },
  "dependencies": {
    "@typescript-eslint/eslint-plugin": "^5.10.0",
    "eslint": "^8.7.0"
  }
}
